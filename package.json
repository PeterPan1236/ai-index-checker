{
  "name": "ai-index-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Scores any public page on how well AI answer engines can crawl, extract and cite it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "cf:dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
